# 复制为 application-local.yml（与本文件同目录，或在项目根目录）。默认开发库已是 root/root，仅在不一致时再用本文件覆盖。
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cashier_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
